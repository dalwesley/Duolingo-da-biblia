/**
 * Validador pedagógico — reexportado para o painel admin (Vite).
 */
export {
  CLONE_ID,
  SKILL_BY_DIFFICULTY,
  VALID_SKILLS,
  validateQuestion,
  validateBank,
  formatReport,
} from '../scripts/_bank_validator.mjs';
