{
  "name": "trilha-admin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "expand:genesis": "node scripts/expand_genesis_complete.mjs",
    "rebuild:genesis1250": "node scripts/rebuild_genesis_1250.mjs",
    "repair:answers": "node scripts/repair_broken_answers.mjs",
    "prepare:content": "node scripts/prepare_content.mjs",
    "migrate:session": "node scripts/migrate_session_pattern.mjs",
    "enrich:content": "node scripts/enrich_content.mjs",
    "seed": "node scripts/seed_content.mjs",
    "seed:cli": "node scripts/seed_content_cli.mjs",
    "seed:refresh": "npm run prepare:content && npm run migrate:session && npm run enrich:content && node scripts/seed_content.mjs",
    "seed:genesis": "npm run expand:genesis && npm run seed",
    "extract:studies": "node scripts/extract_mission_studies.mjs"
  },
  "dependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "@google-cloud/firestore": "^9.0.0",
    "firebase-admin": "^14.2.0",
    "firebase-tools": "^15.27.0",
    "vite": "^6.3.5"
  }
}
